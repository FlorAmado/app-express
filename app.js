const express = require('express');
const app = express();
const port = 3030;

// puerto y callback
app.listen(port, () => console.log(`Servidor levantado en el puerto ${port}`));
//poner en el navegador localhost:3030
//reiniciar servidor para que tome los cambios.




